spring:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

    properties:
      hibernate:
        show_sql: true
        format_sql: true
        generate_statistics: true
        jdbc:
          batch_size: 30
        order_inserts: true
        order_updates: true

